# Resultados experimentales

Esta carpeta contiene solo plantillas mientras no exista un prerregistro OSF válido.

Después de la recolección deben incorporarse o generarse:

- `evaluaciones_expertos.csv` - copia completada de la matriz de evaluación;
- `salida_detector.csv`;
- `consenso_experto.csv`;
- `metricas_detector.csv`;
- `matriz_confusion.csv`;
- `prueba_mcnemar.csv`;
- `kappa_pares.csv`;
- `kappa_fleiss.csv`;
- `desacuerdos.csv`;
- figuras PNG y SVG dentro de `figuras/`.

No edite manualmente métricas o figuras. Deben producirse con `scripts_analisis/run_all.py`.
